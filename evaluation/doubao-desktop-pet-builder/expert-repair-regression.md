# 专家直出回归：已有工程诊断修复

## 输入

把当前工作区原桌宠工程作为只读样本，要求“诊断并修复为稳定、可打包版本”。

## 预先期望

- 默认生成独立修复副本，排除 `node_modules/out/release`；不污染源工程。
- 先静态审计，再按阻断、安全、跨平台、素材和测试分级。
- 应识别：Node 集成与隔离关闭、硬编码盘符、不存在的 `dialog.showInputBox`、提醒结构不一致、初始化顺序、重复 ready、`innerHTML` 风险、主屏假设、吞异常、测试与打包缺失。
- 不应因局部错误无理由推倒所有视觉资产；不应以“代码看起来正确”代替复现。
- 修复后至少有 spec 校验、单元、资产 QA、E2E 和目标平台真人清单证据。

## 思考记录核查

展开每轮思考，逐项标记：路线是否稳定、API 是否真实、结论是否有证据、是否用生图掩盖确定性问题、未实测项是否诚实标成待确认。
